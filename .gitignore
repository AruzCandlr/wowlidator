node_modules/
dist/
.wowlidator/
*.tsbuildinfo

# Healed-selector cache is environment-specific; commit it deliberately if you
# want repairs shared across a team, otherwise leave it local.
healed-selectors.json

# Local provider keys — never commit these.
.env

# Supervision artefacts — one run’s incidents, not the project’s history.
incident.html
incident.jsonl
supervisor.hold

# OS and tool scratch
.DS_Store
.lavish/
scripts-scratch/
CLAUDE.local.md
.claude/skills/wowlidate/monitor/run-state.js

# Local provider keys — the backups too. These hold live credentials, and only
# `.env` itself was ignored, so a `git add -A` would have committed them.
.env.bak
.env.bak-*

# Run artefacts — one run's output, not the project's history.
reports/
